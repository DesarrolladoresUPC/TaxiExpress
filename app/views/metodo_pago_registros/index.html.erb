<p id="notice"><%= notice %></p>

<h1>Relación de Metodos de Pago</h1>

<table>
  <thead>
    <tr>
      <th>Codigo</th>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @metodo_pago_registros.each do |metodo_pago_registro| %>
      <tr>
        <td><%= metodo_pago_registro.codigo %></td>
        <td><%= metodo_pago_registro.nombre %></td>
        <td><%= link_to 'Show', metodo_pago_registro %></td>
        <td><%= link_to 'Edit', edit_metodo_pago_registro_path(metodo_pago_registro) %></td>
        <td><%= link_to 'Destroy', metodo_pago_registro, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Registrar Metodo de Pago', new_metodo_pago_registro_path %>
