<p id="notice"><%= notice %></p>

<h1>Relación de Distritos</h1>

<table>
  <thead>
    <tr>
      <th>Codigo</th>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @distritos.each do |distrito| %>
      <tr>
        <td><%= distrito.codigo %></td>
        <td><%= distrito.nombre %></td>
        <td><%= link_to 'Show', distrito %></td>
        <td><%= link_to 'Edit', edit_distrito_path(distrito) %></td>
        <td><%= link_to 'Destroy', distrito, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Registrar Distrito', new_distrito_path %>
