<%= simple_form_for(@tarifario_registro) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :distrito_origen %>
    <%= f.input :distrito_origen %>
    <%= f.input :tarifa %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
