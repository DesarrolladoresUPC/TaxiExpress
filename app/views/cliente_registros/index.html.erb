<p id="notice"><%= notice %></p>

<h1>Relación de Clientes</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Telefono</th>
      <th>Apellido</th>
      <th>Correo</th>
      <th>Contrasena</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cliente_registros.each do |cliente_registro| %>
      <tr>
        <td><%= cliente_registro.nombre %></td>
        <td><%= cliente_registro.telefono %></td>
        <td><%= cliente_registro.apellido %></td>
        <td><%= cliente_registro.correo %></td>
        <td><%= cliente_registro.contrasena %></td>
        <td><%= link_to 'Show', cliente_registro %></td>
        <td><%= link_to 'Edit', edit_cliente_registro_path(cliente_registro) %></td>
        <td><%= link_to 'Destroy', cliente_registro, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Registrar Cliente', new_cliente_registro_path %>
