<%= simple_form_for(@cuentum) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :correo %>
    <%= f.input :password %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
